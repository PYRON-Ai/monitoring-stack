global:
  resolve_timeout: 5m

route:
  receiver: default
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h

receivers:
  - name: default
    webhook_configs:
      - url: ${ALERT_WEBHOOK_URL:-http://localhost:9000/alerts}
        send_resolved: true

